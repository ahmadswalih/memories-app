import React from "react";
import useStyles from "./Styles";

const Forms = () => {
  const { paper, form, fileInput, buttonSubmit } = useStyles();
  return <div>Forms</div>;
};

export default Forms;
